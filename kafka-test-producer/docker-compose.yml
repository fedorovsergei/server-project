version: '3.9'

networks:
  default:
    name: backend

services:
  kafka-test-producer:
    build: ./
    container_name: 'kafka-test-producer'
    image: 'kafka-test-producer'
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: '512M'
        reservations:
          cpus: '0.10'
          memory: '32M'
